#version 420 core

in vec3 pos;


uniform mat4 modelMat = mat4(1);
uniform mat4 projMat;

out VS_OUT
{
	vec4 color;
	
} vs_out;


void main()
{
	gl_Position = modelMat * vec4(pos, 1.0);
	vs_out.color = vec4(gl_Position);
	
}